# Entorno conda equivalente:  conda env create -f environment.yml
# Activar:                    conda activate metodos-cuantitativos-dti
name: metodos-cuantitativos-dti
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - jupyter
  - notebook
  - pandas
  - numpy
  - scipy
  - statsmodels
  - scikit-learn
  - matplotlib
  - seaborn
  - packaging
  - pip
  - pip:
      - pingouin
      # Técnicas avanzadas (Ciclo 2, Parte 2 — descomenta según la opción):
      # - semopy
      # - factor_analyzer
      # - pymc
      # - arviz
      # - mlxtend
